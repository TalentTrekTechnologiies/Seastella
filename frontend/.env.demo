# Loaded by `vite build --mode demo` (npm run build:demo).
# Builds the frontend-only demo: the API is answered in the browser from seed
# data captured from the real backend, and every screen is labelled "Demo data".
VITE_DEMO_MODE=true
